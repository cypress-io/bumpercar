describe "Circle Provider", ->
  it "uses the given token in each request"

  context "#updateProjectEnv", ->
    it "sets each given environment key"

  context "#runProject", ->
    it "calls the restart api"
